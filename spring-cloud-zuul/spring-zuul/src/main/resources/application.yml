server:
  port: 8087
eureka:
  client:
    service-url:
      defaultZone: http://${eureka.instance.hostname}:8761/eureka/
  instance:
    hostname: localhost
spring:
  application:
    name: zuul
zuul:
  prefix: /api